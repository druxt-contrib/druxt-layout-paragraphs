# Druxt Layout Paragraphs example

This directory contains the Drupal and Nuxt codebases to demonstrate the Druxt Layout Paragraphs module.

## Get started

1. `cd drupal && ddev start && ddev drupal-install`
2. `cd ../nuxt && yarn && yarn dev`
