---
extends: default
rules:
  # 200, not 80: pipeline scripts carry long commands, and wrapping them makes
  # them harder to read rather than easier.
  line-length:
    max: 200
    level: warning
  truthy:
    check-keys: false
